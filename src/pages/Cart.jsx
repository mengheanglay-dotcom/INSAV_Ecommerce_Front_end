import { Link,useNavigate } from 'react-router-dom'
import { useShop } from '../context/ShopContext'
import Icon from '../components/Icon'
import { money } from '../utils/format'
export default function Cart(){const {cart,updateCart,removeFromCart}=useShop();const navigate=useNavigate();if(!cart.items.length)return <section className="page-section"><div className="empty-state cart-empty"><div className="empty-icon"><Icon name="bag" size={30}/></div><span className="eyebrow">YOUR BAG</span><h1>Your cart is empty.</h1><p>Looks like you haven't added anything yet.</p><Link to="/products" className="btn btn-dark">Start shopping →</Link></div></section>;return <section className="page-section cart-page"><div className="page-title"><span className="eyebrow">YOUR BAG</span><h1>Shopping cart <small>{cart.count} items</small></h1></div><div className="cart-layout"><div className="cart-items">{cart.items.map(item=><div className="cart-item" key={item.id}><img src={item.image} alt={item.title}/><div className="cart-item-main"><Link to={`/products/${item.id}`}>{item.title}</Link><span>{money(item.price)}</span><div className="cart-controls"><div className="qty"><button onClick={()=>item.qty>1?updateCart(item.id,item.qty-1):removeFromCart(item.id)}><Icon name="minus" size={15}/></button><span>{item.qty}</span><button onClick={()=>updateCart(item.id,item.qty+1)}><Icon name="plus" size={15}/></button></div><button className="remove" onClick={()=>removeFromCart(item.id)}><Icon name="trash" size={16}/> Remove</button></div></div><strong>{money(item.price*item.qty)}</strong></div>)}</div><aside className="summary"><h3>Order summary</h3><div><span>Subtotal</span><strong>{money(cart.subtotal)}</strong></div><div><span>Shipping</span><strong>{cart.shipping===0?'Free':money(cart.shipping)}</strong></div><div className="summary-total"><span>Total</span><strong>{money(cart.total)}</strong></div><button className="btn btn-dark full" onClick={()=>navigate('/checkout')}>Checkout <span>→</span></button><p>Taxes calculated at checkout.</p></aside></div></section>}
