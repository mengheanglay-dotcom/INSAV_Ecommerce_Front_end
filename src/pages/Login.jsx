import { useState } from 'react';import {Link,useNavigate} from 'react-router-dom';import {useShop} from '../context/ShopContext'
export default function Login(){const {login}=useShop();const nav=useNavigate();const [form,setForm]=useState({email:'',password:''});const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await login(form);nav('/')}catch(err){setError(err.message)}finally{setBusy(false)}};return <AuthLayout title="Welcome back" subtitle="Sign in to manage your orders and checkout faster."><form className="auth-form" onSubmit={submit}><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label>{error&&<div className="form-error">{error}</div>}<button className="btn btn-dark full" disabled={busy}>{busy?'Signing in...':'Sign in →'}</button><p className="auth-switch">New here? <Link to="/register">Create an account</Link></p></form></AuthLayout>}
function AuthLayout({title,subtitle,children}){return <section className="auth-page"><div className="auth-art"><span className="eyebrow light">INSAV SHOP</span><h2>Simple shopping,<br/><em>beautifully done.</em></h2></div><div className="auth-panel"><div className="auth-inner"><span className="eyebrow">ACCOUNT</span><h1>{title}</h1><p>{subtitle}</p>{children}</div></div></section>}
export {AuthLayout}
